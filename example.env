# ==============================================================================
# Environment Configuration Template (example.env)
# Set your environment variables based on these defaults.
# ==============================================================================

# General Application Settings
DEBUG=True
SECRET_KEY=django-insecure-campus-management-change-in-production-key-98234710293
ALLOWED_HOSTS=localhost,127.0.0.1,backend,0.0.0.0

# Database Configuration (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=campus_db
DB_USER=campus_user
DB_PASSWORD=campus_secure_password_2026
DB_HOST=db
DB_PORT=5432

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:80,http://127.0.0.1:3000,http://127.0.0.1:5173

# JWT Configuration
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Frontend Environment Variables (Vite)
VITE_API_BASE_URL=http://localhost:8000/api
